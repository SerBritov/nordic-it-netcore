using System;

namespace Classwork09
{
    class Program
    {
        static void Main(string[] args)
        {
            int x = 0;  //2 операции
            x = x + 2;  //4 операции
            //1 операция на завершение метода
            // итого 7 операций
            //О(N) = 1 -- сводится к единице, т.к. нет зависимости от переменного фактора
            //О(1) 

            //Ω(1) = О(1) = θ(1) -- алгоритм не зависит от количества входных данных
        }
    }
}
